<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Intelligent Store</title>
    <!-- !css文件，集成到ASP.Net之后需要更改路径-->
    <link rel="stylesheet" href="/css/style.css">
    <!-- !从外部导入标签链接 这样就可以直接使用-->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

</head>


<body>
    <!-- 在视图中，将URL作为数据属性添加到一个HTML元素上 方便在js文件里访问该地址-->
    <div id="shoppingCartLink" data-url="@Url.Action("Index", "ShoppingCart")"></div>
    <div id="loginLink" data-url="@Url.Action("Index", "Login")"></div>
    <div id="successfulPay" data-url="@Url.Action("Index", "ShoppingCart")"></div>
    <div id="logoutLink" data-url="@Url.Action("Index", "Logout")"></div>



    <div class="navbar">
        <a href="@Url.Action("Index", "ShoppingCart")">
            <h2>Intelligent Store</h2>
        </a>

        <a href="@Url.Action("Index", "Checkout")">
            <div class="cart">
                <i class="bi bi-bag-check-fill"></i>
                <div id="cartAmount" class="cartAmount">0</div>
            </div>
        </a>

        <a href="@Url.Action("Index", "Login")">
            <div class="cart">
                <i class="bi bi-person-circle"></i>
            </div>
        </a>

    </div>

    <div id="label" class="text-center"></div>

    <div id="history-cart" class="history-cart"></div>


    <script src="/js/userHistory.js"></script>
</body>
</html>

<h2>Hello，this is your homepage.</h2>
